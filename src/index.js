import directive from './directive';
import plugin from './plugin';
import VMaskWrapper from './VMaskWrapper';

export {
  plugin as default,
  plugin as VueMaskPlugin,
  directive as VueMaskDirective,
  VMaskWrapper as VueMaskWrapper,
};
