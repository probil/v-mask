import nodeResolve from 'rollup-plugin-node-resolve';
import babel from 'rollup-plugin-babel';
import uglify from 'rollup-plugin-uglify';


export default {
  input: 'src/index.js',
  plugins: [
    nodeResolve(),
    babel(),
    uglify(),
  ],
  output: {
    format: 'umd',
    name: 'VueMask',
    exports: 'named',
    file: 'dist/v-mask.min.js',
  },
}
